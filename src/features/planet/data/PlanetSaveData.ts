export interface PlanetSaveData {
    level: number;

    posisitonX: number;
    posisitonY: number;

    velocityX: number;
    velocityY: number;

    rotation: number;
    angularVelocity: number;

    isDropPlanet: boolean;
    notUltilCount: boolean;
}
